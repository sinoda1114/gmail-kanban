# 標準CI呼び出し（実体: https://github.com/sinoda1114/ci-standard）
# パッケージマネージャ（pnpm等）は標準CIが lockfile から自動判定する。
name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ci:
    uses: sinoda1114/ci-standard/.github/workflows/node-ci.yml@main
